<Page
    x:Class="EvalToolkit.UI.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="Transparent">
    <StackPanel HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="12"
                Padding="32">
        <TextBlock Text="EvalToolkit"
                   FontSize="32"
                   FontWeight="SemiLight"
                   HorizontalAlignment="Center"
                   Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
        <TextBlock Text="Native Windows companion for the Copilot Evaluation Toolkit"
                   FontSize="14"
                   HorizontalAlignment="Center"
                   Opacity="0.7"
                   Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
        <TextBlock x:Name="VersionText"
                   FontSize="12"
                   HorizontalAlignment="Center"
                   Opacity="0.5"
                   Margin="0,16,0,0"
                   Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
        <TextBlock Text="Step views land in upcoming slices (winui-step1-2 onward)."
                   FontSize="12"
                   HorizontalAlignment="Center"
                   Opacity="0.5"
                   Margin="0,8,0,0"
                   Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
    </StackPanel>
</Page>
