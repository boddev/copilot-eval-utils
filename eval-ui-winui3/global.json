{
  "//": "Pins the .NET SDK feature band used by EvalToolkit so analyzer / compiler updates don't silently break a TreatWarningsAsErrors build. allowPrerelease=false keeps CI on GA bits even when newer previews are installed locally. rollForward=latestPatch (per GPT-5.5 phase-A core-models review) keeps us on patch-level updates only — a 10.0.4xx feature-band bump could ship a new analyzer wave that breaks the warnings-as-errors build, so we don't roll forward automatically across feature bands.",
  "sdk": {
    "version": "10.0.300",
    "rollForward": "latestPatch",
    "allowPrerelease": false
  }
}
